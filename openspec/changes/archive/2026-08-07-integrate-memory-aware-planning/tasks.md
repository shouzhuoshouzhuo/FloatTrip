## 1. 数据模型与投影

- [x] 1.1 为 PlanningBrief 增加投影状态列、动态约束规范化和旧字段兼容解码
- [x] 1.2 实现严格结构化的记忆相关性模型、ID 校验、fingerprint 乐观更新和失败保留
- [x] 1.3 生成统一的 `memory_context`、`effective_constraints` 和 coverage 投影

## 2. Chat 与 API

- [x] 2.1 扩展 Chat 决策 schema 与执行器以维护动态约束和本次记忆排除
- [x] 2.2 扩展 PlanningBrief GET/PATCH/submit 并增加记忆重新匹配接口
- [x] 2.3 让实时 PlanningBrief 事件携带完整投影且保持所有权和幂等校验

## 3. 规划执行与兼容

- [x] 3.1 将有效约束和本次预算冻结到 Travel Plan Run 并扩展 `TravelPlanState`
- [x] 3.2 按类别将约束接入景点、路线、评审、餐饮和提示节点并生成 coverage
- [x] 3.3 在最终 itinerary 中保存约束与覆盖状态，硬约束不可验证时标记 `unverified`
- [x] 3.4 统一 `/api/runs` 与 `/api/plan/stream` 的新规划和修改行程快照语义

## 4. 前端方案卡

- [x] 4.1 将固定偏好输入改为基础字段和动态本次约束编辑
- [x] 4.2 展示自动带入、排除和失败状态，并实现排除、恢复和重新匹配
- [x] 4.3 更新前端状态投影、API 和样式，保持归档及提交状态只读

## 5. 验证

- [x] 5.1 增加投影选择、候选隔离、覆盖优先级、失败和并发测试
- [x] 5.2 增加 API、Run snapshot、规划节点、兼容入口和跨用户测试
- [x] 5.3 增加前端状态测试和旅游场景评测数据
- [x] 5.4 运行后端、前端、OpenSpec 和浏览器回归并修复发现的问题
- [x] 5.5 修复无构建前端的资源版本和缓存头，确保新版方案卡脚本及时生效
- [x] 5.6 增加静态资源缓存与持久化 brief 重载回归，并重新完成页面验收
- [x] 5.7 在方案卡显式展示约束极性、规划效果和语义一致的停用/恢复操作
- [x] 5.8 将 polarity 转换为明确规划指令并增加负向景点的前后端回归测试
