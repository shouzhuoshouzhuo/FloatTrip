## 1. 时间线状态模型

- [x] 1.1 为 `ChatState` 增加稳定的 ActivityItem 派生模型，按消息 sequence、实体 created_at、类型优先级和稳定 ID 排序
- [x] 1.2 在 reducer 中持久维护每个 Run 的产品阶段与已完成阶段，确保内部节点循环不会让进度倒退
- [x] 1.3 增加消息、PlanningBrief、Run 和重试 Run 混排以及重复游标去重的单元测试
- [x] 1.4 增加刷新重建后活动顺序与刷新前一致的单元测试

## 2. 统一活动时间线

- [x] 2.1 新建 ActivityTimeline 渲染层，以统一投影替换 ChatPage 中消息、brief 和 run 的三段列表
- [x] 2.2 为消息、需求摘要、任务和结果定义稳定语义容器，并在实体状态变化时原位更新
- [x] 2.3 实现活动任务离开视口时的轻量摘要与点击定位，避免复制完整任务卡
- [x] 2.4 在多个活动任务下显示目的地或关联行程、任务类型和独立状态，并支持分别定位

## 3. 引导式 PlanningBrief

- [x] 3.1 将 PlanningBrief 默认态改为“这趟旅行，我理解的是”摘要，展示必填字段、已知偏好、默认处理和缺失项
- [x] 3.2 扩展 brief 编辑态以覆盖目的地、日期、天数、预算、餐饮偏好和出行习惯，并复用服务端 readiness 校验
- [x] 3.3 实现日期范围、单选、多选和文本输入的安全控件注册表及未知 schema 文本降级
- [x] 3.4 为保存、清除和正式提交增加请求中禁用、错误恢复与防重复提交行为
- [x] 3.5 增加缺日期追问、可选项默认、编辑后重新校验和明确确认创建 Run 的组件测试

## 4. 规划任务状态与进度

- [x] 4.1 将内部 planning stage 映射到“理解需求、搜集信息、编排优化、完善细节”四个产品阶段
- [x] 4.2 重构 RunCard，使 queued、running、waiting_user、succeeded、failed 和 cancelled 各自显示明确说明与主次操作
- [x] 4.3 在 waiting_user 状态按 input_schema 渲染回答控件，防止重复 resume，并在成功提交后恢复运行展示
- [x] 4.4 为成功 Run 按需获取并缓存权威 itinerary 摘要，展示成果预览、打开完整行程和继续修改操作
- [x] 4.5 为失败、取消和重试关系提供请求保留说明，并将重试显示为关联的新任务而非复活旧任务
- [x] 4.6 区分普通 composer 消息与指定 Run interaction 回复，在回复模式中显示并可清除目标任务上下文
- [x] 4.7 为针对活动不可变 Run 的新约束增加显式决策卡，支持停止后重做、完成后修改或取消本次变更
- [x] 4.8 增加所有 Run 状态、内部循环阶段、等待恢复、完成预览、失败重试与并行任务的前端测试

## 5. 入口与认证连续性

- [x] 5.1 在 App 中保存一次性认证 continuation，并在登录成功后恢复原页面或原动作
- [x] 5.2 将“新建规划”改为创建或复用空的旅行对话，并将“旅行对话”恢复为最近使用对话
- [x] 5.3 保留旧 PlanPage 的受控兼容入口，确认新入口覆盖前不删除旧实现和 API
- [x] 5.4 增加登录成功继续目标、关闭登录不执行目标和新建规划进入新对话的测试
- [x] 5.5 移除“首页”与“新建规划”默认导航，将应用默认、品牌点击和退出登录后统一导向旅行对话；保留仅由行程修改触发的隐藏 PlanPage 兼容路径

## 6. 视觉层级、响应式与无障碍

- [x] 6.1 重构对话页样式，使时间线保持舒适阅读宽度、composer 固定可达且品牌主题继续生效
- [x] 6.2 在窄屏下将对话列表改为可展开导航，并验证时间线、任务卡和输入控件不产生横向溢出
- [x] 6.3 为关键异步状态增加克制的 live region，为卡片标题、表单标签、错误和操作补充可访问语义
- [x] 6.4 实现认证后、等待用户、错误恢复和任务定位后的焦点管理，并确保所有操作可用键盘完成
- [x] 6.5 移除默认界面中的技术 ID、内部节点和“Runtime 在线”等实现细节，改用用户可理解文案

## 7. 验证与迁移

- [x] 7.1 运行前端 reducer、组件、运行时 API 和架构边界测试，修复本变更引入的回归
- [ ] 7.2 手工验证空状态、普通咨询、缺字段、确认、排队、运行、等待回复、完成、失败、取消和多任务完整流程
- [x] 7.3 在桌面与移动端视口验证主要流程的视觉层级、滚动、焦点、状态播报和主题兼容
- [x] 7.4 记录旧 PlanPage 仍被使用的兼容场景，并仅在新对话流达到行为等价后移除默认导航入口
