"""预留给后续自然语言行程生成的智能体边界。"""

from __future__ import annotations


def write_itinerary_narrative(*_args, **_kwargs):
    """把结构化路线计划写成自然语言行程的占位函数。"""
    raise NotImplementedError("行程文案 Agent 不在第一版后端重构范围内")
